### Local development example environment variables
# Copy this file to `.env` and fill values. Do NOT commit your real `.env` to git.

# Postgres container credentials (used by the postgres service)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=auth

# Spring datasource settings (app will fall back to defaults if these are unset)
# When running the app in docker-compose, the host for Postgres should be the service name
# (recommended for local compose):
# SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/auth
# The following line shows localhost for running the app directly on your host. When
# running with compose prefer the 'postgres' host above.
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/auth
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=postgres

# Production/hosted examples (Render/Neon)
# In production, set SPRING_DATASOURCE_URL to the Neon provided connection string.
# SPRING_DATASOURCE_URL=jdbc:postgresql://<neon-host>:5432/<db>
# SPRING_DATASOURCE_USERNAME=<username>
# SPRING_DATASOURCE_PASSWORD=<password>
# Copy this file to .env and fill in the real values (DO NOT commit .env)
# PostgreSQL container configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=auth

# Spring Boot datasource (optional - if you want Spring to use same values)
# For docker-compose use the `postgres` host; for running locally outside of compose
# use `localhost`.
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/auth
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=your_password_here
